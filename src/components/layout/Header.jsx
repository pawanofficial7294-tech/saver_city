import React from 'react';
import Navbar from './Navbar';

// Header acts as the layout entry point. 
// Navbar contains the top bar + sticky navigation.
const Header = () => {
  return <Navbar />;
};

export default Header;
